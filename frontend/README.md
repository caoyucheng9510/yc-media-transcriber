# yc-media-transcriber frontend

本目录是本地 Web UI，使用 Vite、React、Tailwind CSS v4 和 shadcn/ui。

```bash
npm install
npm run dev
npm run build
npm run lint
```

生产模式由 FastAPI 托管 `dist/` 下的静态资源。

更多本地启动、构建和测试说明见仓库根目录的 `docs/development.md`。
